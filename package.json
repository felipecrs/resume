{
  "scripts": {
    "start": "resume serve",
    "build": "mkdir -p public && resume export public/index.html",
    "test": "resume validate",
    "export": "resume export resume.pdf"
  },
  "devDependencies": {
    "resume-cli": "^3.0.6",
    "jsonresume-theme-even": "^0.20.0"
  },
  "volta": {
    "node": "18.17.1",
    "npm": "9.8.1"
  }
}
