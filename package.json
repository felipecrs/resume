{
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf public resume.pdf",
    "build": "mkdir -p public && resumed render --output public/index.html",
    "test": "resumed validate",
    "generate-pdf": "node generate-pdf.js"
  },
  "devDependencies": {
    "jsonresume-theme-even": "0.22.1",
    "puppeteer": "^23.1.0",
    "resumed": "4.0.0"
  },
  "volta": {
    "node": "18.17.1",
    "npm": "9.8.1"
  }
}
